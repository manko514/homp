name: staff
description: "HOMP Staff App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  shared_preferences: ^2.3.2
  geolocator: ^13.0.2
  intl: ^0.19.0
  # firebase_core: ^3.6.0        # Add after Firebase project setup
  # firebase_messaging: ^15.1.3  # See FIREBASE_SETUP.md

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
