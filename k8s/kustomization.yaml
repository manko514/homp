# Apply everything with: kubectl apply -k k8s/
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: homp

resources:
  - namespace.yaml
  - secrets.yaml
  - api-deployment.yaml
  - web-deployment.yaml
  - hpa.yaml
  - ingress.yaml
